# install this git
git clone https://github.com/Mannaja78520/ros_name_sensei_271496_ws.git

# cd in to git repo
cd ros_name_sensei_271496_ws


# build
sudo usermod -aG docker cv
newgrp docker
groups
sudo docker build -t ros2-name-sensei-proj-app .
xhost +local:docker


#run docker
sudo docker run -it \
    --net=host \
    --env="DISPLAY" \
    --volume="/tmp/.X11-unix:/tmp/.X11-unix:rw" \
    --device=/dev/video0:/dev/video0 \
    ros2-name-sensei-proj-app



#run node
ros2 launch name_sensei_proj control_launch.py


#docker ไม่ใช้กล้อง
docker run -it --rm --network host ros2-name-sensei-proj-app
