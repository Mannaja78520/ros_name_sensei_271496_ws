# install this git
git clone https://github.com/Mannaja78520/ros_name_sensei_271496_ws.git

# build
docker build -t ros2-python312-app .

#run

xhost +local:docker
docker run -it \
    --net=host \
    --env="DISPLAY" \
    --volume="/tmp/.X11-unix:/tmp/.X11-unix:rw" \
    --device=/dev/video0:/dev/video0 \
    ros2-python312-app